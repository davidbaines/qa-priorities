// List every style file in styles/ here (one name = one <name>.js file).
// defaultStyle is used on first visit; fallbackStyle must be COMPLETE (every token) —
// other styles may define any subset and inherit the rest from it.
VERSUS.styleList = ["classic", "midnight"];
VERSUS.defaultStyle = "classic";
VERSUS.fallbackStyle = "classic";
